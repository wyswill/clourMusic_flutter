name: flutter_cloudMusic
description: A new Flutter project.

publish_to: 'none' 

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.0
  dio: ^3.0.10
  provider: ^4.3.2+2
  flutter_swiper: ^1.1.6
  shared_preferences: ^0.5.12+4
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

